<h1>我的微博列表</h1>

<table>
  <thead>
    <tr>
      <th>微博内容</th>
      <th>用户</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @microposts.each do |micropost| %>
      <tr>
        <td><%= micropost.content %></td>
        <td><%= micropost.user_id %></td>
        <td><%= link_to '查看', micropost %></td>
        <td><%= link_to '编辑', edit_micropost_path(micropost) %></td>
        <td><%= link_to '删除', micropost, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '新微博', new_micropost_path %>
